# DTU 平台部署记录

- 目标：`43.142.33.210`，入口 `https://i.a-z.xin/`。
- 发布提交：`968327c5285f7fd3abc780f0f01cb3904b6673c5`，本地分支 `codex/dtu-release-20260917`。保留原分支及暂存区，新增独立发布快照。
- 制品：`/opt/iot/releases/968327c/`，包括源代码归档、后端、同版本前端、依赖许可证和校验清单。
- 二进制 SHA-256：`0a8b0b153aaf3088c41d7882988139f350a3183268a204d063412aff1ed92e41`。
- 应用与配置备份：`/opt/iot/deployment-backups/968327c/`，保留旧二进制、旧静态目录入口、Nginx 和服务配置，以及约 38 MiB 的数据库完整逻辑备份。备份目录权限 0700，敏感备份文件 0600。

## 验证

Linux 沿用现有 Arch Clang 22.1.8 Release 构建树；与 CI Ubuntu Clang 19 环境不同。目标主机架构、动态依赖及加载器检查通过。Linux CTest 35/35 通过。Windows Release 构建通过，按当前工作流明确排除 `gb28181-sip` 的测试范围通过 34/34；单独运行该用例仍因 IPv6 UDP catalog 未收到数据失败，不记为通过。

前端类型检查、lint、生产构建、Biome 检查通过；页面架构、HTTP/SSE 契约、快照流及节点抽屉测试 23/23 通过。DTU HTTP/WS 隔离集成测试已在本地独立数据库和 Redis 验证。

部署约 7.1 秒恢复就绪。`iot.service` 为 active/running，异常重启次数 0。数据库新增 `0051_edge_dtu`，总迁移数 54，原 53 条迁移 ID 和校验和不变。TimescaleDB、Redis 健康，Redis AOF 启用且写入状态正常。后端继续只监听 `127.0.0.1:3000`，生产 `.env` 权限为 0600。

通过校验证书的 HTTPS 逐一核对新前端资源哈希，真实浏览器正常显示线上登录页面。验收时 12 个节点会话恢复，设备数据由部署前 308143 条增长到 308149 条。启动日志未出现 fatal、panic、segmentation、failed 或 error。

此次仅部署平台，未发布或刷写 EdgeNode 固件。现有固件未声明 DTU 能力时，平台禁止为其保存透传配置；需使用后续支持 DTU 的节点固件。未执行生产登录后的 DTU 配置写入或现场串口透传验收。

## 注册包、心跳包及 ASCII/HEX 补充发布

补充版本 `1055463639babb6429161e97aee441c36bebd26e` 已部署到 `/opt/iot/releases/1055463/`，前一版本和数据库备份保留在 `/opt/iot/deployment-backups/1055463/`。二进制 SHA-256 为 `f6fd59ef3e56f818c2b356f155c63c5152e3d73ed16cf66acbc48a1f7906e4d2`。

注册包、心跳包独立提供 ASCII/HEX 输入切换，统一保存字节；心跳间隔 1～86400 秒，0 关闭。节点实现注册优先、心跳定时发送、断线重连重新计时及独立调试标记。本次仍未构建或刷写目标节点固件。

Linux Release 和 CTest 35/35、Windows Release 和 CI 测试范围 34/34、固件主机测试 22/22 通过。新增配置测试 3/3，HTTP/WS 集成通过。使用锁定 Bun 1.3.14 完成前端检查、构建；浏览器验证 `DEV001`、`PING` 的字节转换、保存回读和 390px 窄屏。发布后约 7.38 秒就绪，迁移 applied=0、skipped=54，服务无异常重启；HTTPS 静态文件与制品逐一匹配，采集数据继续增长。

补充发布后确认 11 个有效节点会话。另一个节点最后上报为 `2026-09-17 08:46:19 UTC`，早于本次切换，并非本次重启后才停止上报。
