# EdgeNode 862538065243970 线上核查

核查对象：`https://i.a-z.xin/`，节点“开发测试”，TAS-682F IT692，EdgeNode 0.3.38。时间均为北京时间，2026-09-07。

## 现场证据

- 17:31 左右页面仍显示在线，但最后上报停在 17:11:50。
- 17:32:39 Web Terminal 打开超时；未建立终端，未向设备发送 shell 命令。
- 17:33 左右页面转为离线，终端入口禁用。
- 数据库 `last_seen_at` 与 `updated_at` 均为 `2026-09-07 09:11:50.833416+00`。
- 17:35 左右 Redis 会话键不存在，`EXISTS=0`、`TTL=-2`，与离线页面一致。
- 相关时间段日志出现 Broken pipe、WebSocket close/read overlap、EOF，但日志未标明节点 ID，不能据此确定这些错误均属于本节点。

本次未重启服务、修改线上配置、部署程序或升级固件。数据库、Redis 和页面确认的是核查时状态，不代表设备此后持续离线。

## 本地修正与验证

本地网关原先在 socket 对象存在时定期续租，不能证明设备仍能接收和回复应用消息。改为仅由真实入站消息续租，并每 20 秒发送应用层 Ping；连续 60 秒无入站消息则终止连接。90 秒 Redis 租约负责进程异常退出后的兜底，租约到期产生页面更新事件。WebSocket 传输层仍保留 30 秒 Ping / 15 秒 Pong 超时。

已核对 0.3.38 固件源码支持应用层 Ping/Pong；隔离环境实际 WebSocket 测试覆盖协议版本 2、5、6：响应节点持续在线，只有传输层 Pong 而不回复应用消息的节点约 60 秒断开。旧遥测 protobuf 字节兼容测试通过。这些是本地验证，尚不能断言其修复了本次线上异常的全部原因。
