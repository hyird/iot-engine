# iot-engine 协作规范

## 1. 基本规范

### 1.1 修改原则

- 适用于全仓库，用户最新明确要求优先。
- 文档用中文，路径、命令、标识符保留原文。
- 修改前检查 `git status`，保留其他改动。
- 删除前确认无实际用途；不留空目录、占位文件、转发头或旧名称别名。

### 1.2 命名与变更

- 命名明确对象、职责、动作，不重复上下文。
- 避免含糊缩写、冗余前缀及 `handle`、`process`、`doWork` 等泛称。
- 不得用 `Manager`、`Helper`、`Processor`、`Data` 混合无关职责。
- 保留行业术语与框架约定，不对第三方源码进行风格性改名。
- 重构保持 API、协议、权限码、DTO、表名、历史迁移、产品及安装身份。
- 必须改变外部契约时，单独定义并验证迁移方案。
- 移动、改名、删除时，同步更新调用、装配、构建、检查、测试及文档。
- 按实际行为验收，不以命名、目录或编译通过代替验收。

## 2. 项目结构

技术栈：后端 C++23 与 Ruvia，前端 React，数据库 PostgreSQL/TimescaleDB，消息通信 Redis。

### 2.1 目录结构

```text
service/
├── modules/
│   ├── <module>/           # 管理 API 与业务操作
│   └── system/<module>/    # 系统管理模块
├── features/
│   └── <feature>/          # 后台任务、设备协议、消息消费
│       └── <component>/    # 独立子组件，最多一级
├── common/                 # 公共契约与基础定义
├── config/                 # 进程配置与数据库迁移
├── middleware/             # 认证、权限、日志
├── utils/                  # 无业务归属的无状态工具
└── server.cpp              # 装配、注册、生命周期
web/
├── pages/<module>/         # 页面业务模块
├── components/             # 跨页面 UI
├── layouts/                # 页面布局
├── routes/                 # 路由
├── providers/              # 上下文与依赖接入
├── store/                  # 客户端共享状态
├── hooks/                  # 通用 Hook
├── config/                 # 前端配置
├── types/                  # 公共类型
├── lib/                    # 第三方库接入
├── utils/                  # 无状态工具
├── styles/                 # 框架、主题、全局样式入口
└── public/                 # 静态资源
clients/<platform>/         # 平台客户端与 CMake 入口
ports/                      # 依赖构建适配
tests/                      # 单元、契约、集成测试
.github/workflows/          # CI 工作流
build/                      # 构建、后端生成代码、临时验证产物
```

- 按实际业务归属划分，同一业务的页面和接口归同一模块。
- 无菜单功能归所属业务，不按 URL 分层，不创建空的前后端镜像目录或无独立职责的 `iot/` 分组。
- 目录用小写 `snake_case`，文件用职责前缀。
- 禁用裸名 `service.h`、`runtime.h`、混合大小写或连字符。
- 私有实现留在模块，确有跨模块复用再提取。
- 共享目录不收纳页面业务或归属未定的代码，不增加平行业务层。
- 后台子组件须有独立协议、传输或运行职责，不能为缩短文件拆分或继续嵌套。
- `common/` 不建子目录，不含运行对象或业务实现。
- 消息契约放 `common/message.h`，仅含格式、键名、版本及无 I/O 编解码。
- 源码、依赖、生成产物分离。
- 前端生成路径遵循生成器配置，不手工修改生成文件。
- 临时脚本、日志、测试数据不放入业务目录。

### 2.2 文件职责

`<name>` 为所属模块或组件名。仅创建实际需要的文件，同一职责集中在一个文件中。

#### 后端文件

| 后端文件 | 适用范围 | 职责 |
| --- | --- | --- |
| `<name>.entity.h` | `modules`、`features` | 实体与存储映射；有数据访问时必需 |
| `<name>.service.h` | `modules`、`features` | 业务规则、业务权限、查询、写入、事务 |
| `<name>.types.h` | `modules`、`features` | DTO、请求模型及字段校验、领域值、内部状态；无 I/O |
| `<name>.error.h` | `modules`、`features` | 稳定错误 |
| `<name>.controller.h` | `modules` | 路由、参数、中间件与响应 |
| `<name>.runtime.h` | `features` | 启停、调度、消费、生命周期 |
| `<name>.config.h` | `features` | 配置类型、解析、默认值；无业务数据库访问 |
| `<name>.protocol.h` | `features` | 编解码、帧解析、协议状态；无数据库或网络 I/O |
| `<name>.transport.h` | `features` | 网络、设备、SDK 适配；无业务数据库访问 |
| `<name>.proto` | `features` | Protobuf 消息 |

- 后台 `runtime`、`protocol`、`transport` 可使用同名 `.cpp`，其余使用头文件实现。
- include 使用仓库根相对路径 `service/...`。
- 优先类内方法，仅保留必要的 `inline`，模板不重复标注。
- 禁止白名单外文件及变体，包括 `repository`、`queries`、`helper`、私有 `common` 及多个 `service` 变体。
- 模块及后台子组件不建私有子目录。

#### 前端文件

| 前端文件 | 职责 |
| --- | --- |
| `index.tsx` | 页面入口与私有 UI |
| `<name>.types.ts` | 类型与 DTO |
| `<name>.schema.ts` | 请求、表单校验 |
| `<name>.api.ts` | HTTP 客户端、请求地址与 SSE 接入 |
| `<name>.service.ts` | 业务操作、查询、服务端缓存 |

- 前端模块不增加其他私有文件或子目录。
- 页面通过 Service 获取数据，请求方法及地址集中在 `<name>.api.ts`。

### 2.3 依赖边界

| 调用方 | 允许依赖 |
| --- | --- |
| 页面 | 所属 `service`、`types`、`schema`、公共 UI 与状态接口 |
| `controller` | 所属 `service`、`types`、`middleware` |
| `runtime` | 所属 `service`、`protocol`、`transport`、`config` |
| `service` | 所属实体与类型、同层公开接口、公共契约与工具 |
| `protocol`、`types`、`entity` | 无 I/O 类型与工具 |
| `transport` | 协议类型、网络、SDK |
| `server.cpp` | 公开装配入口 |

- 依赖无环，不访问其他组件内部状态；公共代码不反向依赖业务或页面。
- Controller 负责请求接入，Runtime 负责运行驱动，业务与数据库操作归 Service。
- Service 不依赖 Controller 或 Runtime，不接管生命周期。
- `modules` 与 `features` 互不引用或调用，仅通过 Redis 消息或数据库交互。
- 禁止借共享层、回调、全局对象、装配入口绕过跨层边界。
- 跨页面只依赖其他模块公开的 `service`、`types`。
- 会话状态仅负责状态读写，认证刷新归所属 Service，在应用入口注入请求层。
- 验收检查实体遗漏、无用声明、职责混放、跨层引用、依赖环、生成产物及无用文件。

## 3. 数据访问与迁移

### 3.1 实体归属

- 每个访问数据的模块或组件仅有一个 `<name>.entity.h`。
- 无数据访问时不创建实体文件。
- 数据访问包括只读、联表、事务、投影、Redis 业务记录与缓存。
- 使用原生 SQL 或 Redis 命令仍须遵守实体归属规则。
- PostgreSQL/TimescaleDB、Redis ORM 及持久化映射集中在所属实体文件，不按表拆分或散放。
- 实体须实际用于查询、映射或写入，准确覆盖对象、类型、主键、空值、默认值。
- 仅使用字符串表名、列名的 `DbQuery` 不视为完成实体迁移。
- 可复用同层公开实体，但不能寄放自身映射或省略必需实体文件。
- `modules` 与 `features` 各自维护映射，禁止跨层或共享 ORM 绕过边界。
- 区分实体、DTO、协议、消息；队列、锁、通知不伪装成表。
- ORM 无法表达时，在实体文件定义实际映射，记录具体缺口、调用及保留原因。

### 3.2 查询与事务

- 使用固定版本 Ruvia 公开 ORM、Query、Schema API，不加转发包装、兼容接口或未经验证的替代方案。
- SQL 输入参数化，保持事务、锁顺序与幂等语义。
- Redis 保持原子性、过期、重试、确认语义。
- 必要持久化后才确认，缓存不能掩盖数据库失败。

### 3.3 迁移与字段

- 迁移统一放在 `service/config`。
- 未经验证不改已执行 ID、校验和、历史 SQL。
- 迁移重构验证新库、升级、重复执行、漂移拒绝、失败回滚。
- `DbColumnOptions` 的 `enumName`、`defaultExpression` 准确描述枚举及默认值。
- 枚举按文本读写，物理类型与历史默认值归迁移，补充元数据不得修改历史迁移或实际列类型。
- DTO、实体、校验、数据库约束一致，明确空值、可选、默认、清空语义。
- Ruvia 字段必填与可空独立：`RUVIA_OPTIONAL_FIELD` 仅允许缺失，允许 null 须显式添加 `RUVIA_NULLABLE`。`RUVIA_DEFAULT` 只应用于缺失的可选字段，不能满足必填约束，显式 null 不应用默认值。PATCH 用公开 `isPresent`、`isNull` 区分缺失、不修改与 null、清空，不为判断存在性重新解析原文。此前明确接受 null 按缺失处理的字段须显式标记可空并保留对应测试。
- `JsonValue`、`JsonObject` 字段不附加字段校验规则；固定字段使用具体类型，动态内容的业务约束归 Service。`fromJson` 仅解析结构，不替代路由字段校验。
- 独立 Collector 使用公开 `RedisClient(EventLoop, RedisConfig)`，每个 Worker 独占客户端。停止业务任务后在所属 EventLoop 等待 `shutdown()`，再关闭线程；不使用私有 `RedisRegistry` 或新增 Redis 转发包装。
- 请求 Model 能直接供 Service 使用时，不保留字段相同的输入 struct 和中间转换；独立领域类型须有实际业务或所有权职责。
- 数据库及连接用 UTC，时间用 `TIMESTAMPTZ`。
- 设备时间按其 `timezone` 入库，API 明确输出 UTC。

## 4. HTTP 接口与 SSE 推送

### 4.1 请求方式

| 场景 | 方式 |
| --- | --- |
| 普通查询 | HTTP GET |
| 创建或执行操作 | HTTP POST |
| 更新 | HTTP PUT/PATCH |
| 删除 | HTTP DELETE |
| 登录、认证刷新、上传 | 各自明确的 HTTP 接口 |
| 设备、运行状态、告警等实时变化 | SSE |
| 串口、终端、固件协议的双向流 | 专用 WebSocket |

- 普通业务不得使用统一 WS，也不得将所有 GET 改为 SSE。
- 同一页面可用 HTTP 查询、保存，用 SSE 接收变化；非实时数据不建订阅。
- 按需使用 SSE，禁止过度 SSE 化；新增订阅须明确实时展示需求和事件来源。
- 配置管理、表单详情、选项、历史记录使用 HTTP，不因数据可能变化就建立 SSE。
- 用户资料通过 HTTP 获取，不为公共布局单独维持常驻 SSE。
- 权限变化可随已有实时连接推送；服务端始终独立校验权限。
- 用户操作直接返回 HTTP 结果，不等待 SSE 回执，不封装为 WS 事件。
- Controller 使用 Ruvia 公开 HTTP 路由 API，接收类型化参数并调用所属 Service。
- 后端字段校验直接绑定 `types.h` 中的 Ruvia 请求模型，不创建模块级 `schema.h` 或独立 Validator；创建、更新约束不同时使用独立模型。跨字段业务规则与数据库校验归 Service。
- 普通 HTTP handler 返回 `ruvia::Task<ruvia::HttpResponse>`，响应模型通过 `c.json(model)` 显式序列化；Service 和内部查询可返回 `Task<Model>`，无返回值的任务显式使用 `Task<void>`。模型必须在所属内存作用域释放前序列化，不保留默认返回类型别名或兼容包装。
- 响应明确状态码与业务结果；鉴权、权限、校验、错误处理接入中间件。
- 所有自有管理 API 的接口权限码统一在路由声明中绑定 `service::middleware::PermissionMiddleware<"权限码">`；认证先于权限检查，权限检查先于模型校验和业务调用。Controller 普通 handler 不再调用 `requirePermission` 或自行查询接口权限；不得按模块保留新旧两套接入方式。
- 接口权限中间件不能替代 Service 的业务权限：资源归属、租户或数据范围、共享授权、状态相关限制仍由所属 Service 校验。多权限、条件权限必须保持原有逻辑，不得简单合并成更宽松的单权限检查。
- SSE、WebSocket 连接建立时校验权限，连接期间保留事件查询、命令执行所需的权限复核及撤销处理，不能只在握手时检查。第三方接口、固件和专用令牌认证保持各自原有授权契约，不强制套用管理端登录权限。
- 权限迁移覆盖全部自有管理模块，逐路由核对原权限码、允许与拒绝条件、校验顺序及响应；验证未登录、无权限、合法权限、权限撤销和业务数据越权，不能仅凭删除调用或编译通过认定完成。
- 升级 Ruvia 时，全项目按固定版本公开 API 迁移。固定结构请求、配置、消息及状态优先使用所属 `types.h` 或公共消息契约中的 Model；多协议配置分别建模，需原文入库时使用公开的 `ValidatedJson<T>::raw()`。真正包含运行时键名、任意 JSON 值或未知字段保留需求的结构，可使用固定版本已公开的 `JsonValue`、`JsonObject`；不得因此回退已完成的固定结构 Model 迁移。动态 JSON 视图借用输入，输入字符串及解析资源须覆盖全部使用期，不得传入临时字符串或让视图逃逸。不得引用框架 `detail` 解析器、在项目内恢复旧 API 或添加兼容包装；公开 API 仍无法表达的需求须先列明具体契约和能力缺口，再确定方案。

### 4.2 事件驱动

- 仅由真实业务事件触发推送：写入提交成功、设备数据或状态变化、权限变化。
- 无变化不发业务事件；禁止重复快照、空业务事件和未变化的数据。
- 禁止定时查询后比较差异，伪装成事件驱动。
- 首次订阅发送一次初始快照；断线恢复可补发遗漏事件或发送一次恢复快照。
- 后续仅查询、推送受事件影响的数据，不因单个指标变化刷新整页无关数据。
- SSE 通过 TanStack Query 更新缓存，仅更新受影响的数据与 UI。
- 高频事件合并同一批变化，避免无关查询失效和全量重绘。
- 连续数据保证顺序与背压，不静默丢失必须交付的数据。

### 4.3 订阅与连接

- 每个页面最多一条 SSE；页面内组件、弹窗、抽屉共享连接，按事件类型分发数据。
- 设备列表、分组计数、实时指标共用一条 SSE，按事件类型更新各自缓存。
- 指标变化仅更新指标；配置或分组变化才更新相关列表、分组。
- 无实时需求的页面不建 SSE；不同页面不强制共用一条连接。
- 仅订阅当前视图需要的实时数据；调试、日志、进度视图关闭后取消对应订阅。
- 最后一个实时视图关闭后释放连接，不保留兜底 SSE。
- 查询条件改变订阅范围时，取消旧订阅，按新条件建立订阅。
- 断线允许有界退避重连；正常连接不得靠重复重连刷新数据。
- 确需保活时，只发送 SSE 注释心跳，不携带业务数据。
- 心跳不触发业务查询、前端业务事件或缓存更新。
- 未造成数据变化的通知不得触发心跳。
- 取消订阅、离开页面、退出登录、断线时，清理所属 Worker 的资源。
- SSE 失败时显示连接状态，允许手动重试，不降级为轮询。
- SSE 失败不改变 HTTP 查询、写入方式；恢复后继续推送变化。

### 4.4 禁止轮询

- 前端禁止定时 GET、定时重新订阅及其他周期性业务查询。
- TanStack Query 全局配置和单个查询均禁用 `refetchInterval`。
- 禁止通过 `setInterval`、递归 `setTimeout`、定时缓存失效或重挂载间接轮询。
- 服务端不得为生成 SSE 定时查询全量或增量数据。
- 实时缓存由事件更新，不靠周期性失效、重新请求维持实时性。
- 允许页面进入、筛选、分页、手动刷新或明确数据变更触发 HTTP 查询。
- 写入成功后可按需刷新受影响查询。
- 用户操作触发的一次查询不属于轮询，仍使用 HTTP 返回结果。

### 4.5 兼容与清理

- 通信改造仅限自有管理界面与客户端。
- 第三方接口保持现有 HTTP 方法、地址、响应及订阅协议。
- 保留已部署 EdgeNode 固件的协议兼容性。
- 删除统一业务 WS 入口及专属事件封装、兼容分支、旧测试假设。
- 测试改为验证真实 HTTP/SSE 行为；保留有效的资源清理、权限和上传恢复实现。

### 4.6 验收

| 场景 | 要求 |
| --- | --- |
| 页面及弹窗、抽屉同时使用 | 活跃 SSE 总数不超过一条 |
| 无实时需求、最后一个实时视图关闭 | 无活跃 SSE |
| 页面空闲、重复通知 | 无业务轮询、无重复业务推送 |
| 单项变更 | 仅更新相关事件与缓存 |
| SSE 断线 | 无周期性业务查询、无轮询降级 |
| 断线恢复 | 数据完整，不重复建立连接 |

- 使用真实浏览器网络记录及服务端查询、事件证据验证。
- 不得仅凭代码使用 SSE 就认定通过。

## 5. Worker 与生命周期

- 保留 Service Worker、Collector Worker 两类；同类装配、注册、职责、流程一致，仅编号及实际持有连接不同。
- Service 处理接口与业务。
- Collector 持有 GB28181 SIP 接入、注册、心跳、会话及控制，两类经 Redis 指令与回执交互。
- 无全局 SIP 运行时，不绕过 Ruvia 公开接口。
- 各 Worker 独占组件、数据库连接、Redis 连接、网络连接、缓存、会话、订阅、定时器和任务状态。
- 连接从接入到关闭，解析、执行、SDK 调用、结果及响应发送均归原 Worker。
- 禁止共享可变对象、跨 Worker 调用、回调、内存转发或连接转交。
- 不得按设备 ID、哈希、分片、轮询改派，也不得借助 Redis 或数据库转交同类 Worker 的工作。
- 禁止 `workers.front()`、固定下标、Worker 0 特殊职责。
- 恢复与清理按实际归属执行。
- 无连接任务用相同机制独立认领，由认领者完成，通过事务和幂等机制避免重复执行，不按编号或哈希分工。
- ZLMediaKit 可有独立运行时与 SDK 线程，但业务状态及异步结果归发起 Worker，不共享业务状态。
- 其他 SDK 或全局资源的隔离障碍须记录并解决。
- 删除旧路由、分片、兼容队列消费、迁移分支、失效接口及测试假设；保留必要固件兼容路径。
- 不保留新旧并行实现。
- 覆盖重启、缩容、异常退出、幂等、过期清理。

## 6. 前端规范

| 用途 | 工具 |
| --- | --- |
| 服务端状态 | TanStack Query |
| 客户端状态 | Zustand |
| 校验 | Zod |
| 虚拟列表 | `@tanstack/react-virtual` |
| 排序 | `@dnd-kit` |

- 锁定 Bun，安装用 `bun install --frozen-lockfile`。
- 升级依赖用最新稳定版，删除前检查源码、构建、运行用途。
- selector、Hook 默认值、effect 依赖保持引用稳定；路由提供友好错误兜底。
- 左侧树负责导航或筛选，右侧平面表格；表头、分页固定，表格内部滚动，验证窄屏和横向滚动。
- 表单用 `FormModal`：默认宽 720px，高 `min(720px, 90dvh)`。
- 表单标题、操作区固定，内容独立滚动，标签纵排。
- 复用组件与工具类；指标统一 CSS Grid；虚拟列表切换数据后重新测量并恢复滚动位置。
- `web/styles/index.css` 仅框架导入、层声明、主题变量。
- 主题用 Ant Design 或 Tailwind 配置，不加组件覆盖样式。

## 7. 客户端与固件

### 7.1 Windows 客户端

- UI 经控制器与注入接口调用连接能力，只依赖 IPC，不导入服务或 WireGuard 运行对象。
- 连接 Service 管理会话、状态、撤销恢复与重试。
- 平台适配使用 HTTP/JSON 与 SSE，禁止业务轮询。
- 状态存储负责持久化与加密。
- WireGuard 适配负责隧道与密钥。
- 各组件不得访问其他组件内部状态。
- 共享代码限实际复用的产品与 IPC 契约、文本、文件、加密基础操作。
- 状态文件与隧道生命周期归服务。
- 移动实现同步更新原生服务及 WinUI 构建，保持产品、服务、IPC、凭据及安装身份。
- VPN 断开、注销、删除、撤销后回收地址。
- 重连重新分配、优先复用空闲地址，已撤销 Peer 不得占用地址池。
- 释放失败保留重试身份，迟到请求不影响新连接，复用前在 Hub 移除旧 Peer。

### 7.2 EdgeNode 固件

| TAS-682 构建项 | 配置 |
| --- | --- |
| 主机 | `10.10.0.101` |
| 目录 | `/home/openwrtbuild/immortalwrt-dtu` |
| 用户 | `openwrtbuild` |

- 构建前遵守固件仓库的 `AGENTS.md`。
- 协议、API、配置、任务、升级兼容已部署固件，保留 `0.3.44` 路径。
- 移除兼容路径须有测试通过的迁移方案、兼容窗口及用户明确批准。
- 支持分块的固件使用 WS，限制块大小并支持断点续传。
- 旧固件保留带令牌的直接下载。
- 协议兼容不豁免结构和依赖规则。

## 8. 验证与交付

| 改动 | 必须验证 |
| --- | --- |
| 文档 | `git diff --check` |
| 前端 | 类型检查、lint、生产构建、修改文件 Biome 格式检查 |
| 后端 | Release 构建、CTest |
| 布局 | 实际页面、窄屏、滚动 |
| CI | 工作流、缓存、制品路径 |

- 如实说明未覆盖检查。
- 本地仅用根 `build/`，CI 可用 `build/ci/`。
- 可执行文件与同级 `web/` 同版本交付。
- Windows 客户端依赖、构建、测试、安装包统一经 CMake。
- `clients/` 不留 `.ps1` 构建脚本，保留依赖锁定、校验及打包验证。
- Ruvia 固定完整提交 SHA，升级统一迁移公开 API，不增加过渡宏、包装接口或兼容头。
- vcpkg 固定提交。
- 工具链以 `.github/workflows/build.yml` 为准，缓存区分平台、架构、编译器、依赖输入。
- CI 仅支持 Linux、Windows；发布须两平台构建与测试通过，制品同一提交。
- 单平台例外须用户明确接受，并记录未覆盖平台。

## 9. 部署规范

| 项目 | 配置 |
| --- | --- |
| 主机 / 域名 | `43.142.33.210` / `i.a-z.xin` |
| 入口 | Nginx 终止 TLS → `127.0.0.1:3000`；后端不监听公网 |
| 服务 | `iot.service`；目录 `/opt/iot`；入口 `server`；静态目录 `web/` |
| 数据库 | 独立 `iot_engine`，不改旧库 |
| Redis | `127.0.0.1:6379`；AOF；`/opt/redis/data` |
| 镜像源 | 直接拉取官方镜像，不使用镜像代理；TimescaleDB 使用 `timescale/timescaledb:latest-pg18`，Redis 使用 `redis:latest`，由 Compose 管理 |
| 制品目录 | `/opt/iot/releases/<短 SHA>/` |

- 凭据不得写入提交、制品、日志或文档；生产 `.env` 权限 `600`，密钥在服务器生成。
- Nginx 以 `http` 用户直接提供 `/downloads/`，`/opt/iot/releases/<短 SHA>/` 与其中 `web/` 必须 `755`，禁止用 `umask 077` 创建前端目录。
- 切换前核验哈希、架构、依赖、静态入口，备份二进制、静态文件、Nginx 配置。
- 切换后检查服务、迁移、数据库、Redis、Nginx、证书、实际前端资源，以及 `https://i.a-z.xin/downloads/iot-egine-Setup-x64.exe` 返回 200 且支持 Range。
- 关键检查失败立即回滚，验证完成前保留备份。
